import { Component } from '@angular/core';
import { SEMANTIC_COMPONENTS, SEMANTIC_DIRECTIVES } from "ng-semantic";

@Component({
    selector: 'my-app',
    templateUrl: `app/components/heros/hero.component.html`
})
export class HeroComponent {}